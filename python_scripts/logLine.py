def logLine(logLines, newString):
    # appends logLines array with newString 

    # Returns:
    # logLines array 
    logLines.append(newString)
    print(newString)
    
    return logLines